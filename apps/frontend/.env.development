# Solo para `next dev`: el backend FastAPI corre en el puerto 3001.
# En producción el build estático se sirve desde el propio backend y
# NEXT_PUBLIC_API_URL se define al buildear (Dockerfile / build arg).
NEXT_PUBLIC_API_URL=http://localhost:3001
